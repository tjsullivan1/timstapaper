# k8s/manifests/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: timstapaper
  namespace: timstapaper-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: timstapaper
  template:
    metadata:
      labels:
        app: timstapaper
    spec:
      containers:
      - name: timstapaper
        image: tjsullivan1/timstapaper:latest # Use a specific tag for better control
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_PATH
          value: "/data/timstapaper.db"
        - name: GOOGLE_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: google-creds
              key: google-client-id
        - name: GOOGLE_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: google-creds
              key: google-client-secret
        volumeMounts:
        - name: data-storage
          mountPath: /data
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: timstapaper-data-pvc